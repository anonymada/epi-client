import { AddListAttributeDirective } from './add-list-attribute.directive';

describe('AddListAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new AddListAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
